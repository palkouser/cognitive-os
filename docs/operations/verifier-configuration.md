# Verifier configuration

Copy `config/verifiers.example.yaml` to ignored `config/verifiers.local.yaml`. Timeouts and all size limits must be positive and bounded. `parallel_execution: true`, unknown fields, custom unit-definition paths, and unrestricted solver options are invalid. The file must contain no credentials.
