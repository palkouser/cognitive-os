# MCP diagnostics

Use `scripts/mcp_health.py` for safe availability, `scripts/mcp_list_tools.py` for discovery, and
`scripts/mcp_call_tool.py` only after policy and approval review. Diagnostics print identifiers and
counts, not environment values or raw private output.
