# Tool configuration

Copy `config/tools.example.yaml` to ignored `config/tools.local.yaml`. Configure canonical workspace
roots and enabled tools. Network-enabled sandbox limits and paths outside permitted roots are unsafe
and rejected by policy.
