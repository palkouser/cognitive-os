# MCP configuration

Use ignored `config/mcp.local.yaml`. Each enabled server needs an absolute existing executable,
argument array, permitted working directory, and minimal environment allowlist. Do not configure
`npx`, `uvx`, package managers, remote transports, or inherited secrets.
