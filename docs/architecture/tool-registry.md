# Tool Registry

The registry explicitly binds immutable, versioned descriptors to implementations. Duplicate
identities fail, provider-visible entries are filtered and sorted, snapshots are deterministic, and
the registry cannot change after freezing.
