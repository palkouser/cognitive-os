# Mathematics verifiers

The parser accepts numeric literals, symbols, safe arithmetic, equality, seven allowlisted functions, and two constants. It rejects attribute access, subscriptions, imports, lambdas, comprehensions, keyword arguments, and unknown functions. Node, depth, symbol, digit, exponent, and worker-time limits are enforced. Numeric tolerance is exact-decimal based; symbolic unknown remains unverifiable.
