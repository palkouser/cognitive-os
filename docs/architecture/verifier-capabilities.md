# Verifier capabilities

Capabilities bind subject type, problem domain, criterion type, media types, sandbox and network requirements, and batch support. Sprint 7 built-ins are single-subject. Selection order is explicit verifier, exact capability, configured priority, then ambiguity error. Historical success, provider advice, latency, and cost never influence selection.
