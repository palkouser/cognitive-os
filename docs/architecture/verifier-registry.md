# Verifier Registry

Registration is explicit by `(verifier_id, version)`. Descriptor hashes, JSON configuration schemas, implementation identity, optional availability, and capabilities are validated before freeze. Sorted snapshots produce a stable SHA-256 identity. There is no import-time global registry.
