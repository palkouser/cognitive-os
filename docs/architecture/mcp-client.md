# MCP client

Sprint 5 supports the stable MCP v1 SDK with explicitly configured local STDIO servers. Commands
are absolute argument arrays, environments are allowlisted, startup is explicit, and package
installation is forbidden. Discovered tools are namespaced and default to R2.
