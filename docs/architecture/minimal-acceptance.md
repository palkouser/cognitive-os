# Minimal acceptance

Sprint 6 performs structural checks for JSON Schema, verified artifact existence, required
step completion, successful tool completion, and plan consistency. Every required criterion
must pass. Manual and domain-verifier criteria are recorded as unverifiable and block
completion when required. Provider confidence and completion claims are ignored. The full
Verifier Registry and semantic domain verification are Sprint 7 scope.
