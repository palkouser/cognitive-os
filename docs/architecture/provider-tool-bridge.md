# Provider-to-tool bridge

Only filtered registry descriptors become provider tool definitions. Normalized provider calls are
mapped to controlled invocations and executed sequentially. Results become validated tool messages;
Sprint 5 does not automatically call the model again.
