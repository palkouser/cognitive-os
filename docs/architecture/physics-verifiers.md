# Physics verifiers

Physical quantities contain finite decimal magnitude, controlled unit text, and optional uncertainty. Dimension, quantity, and conversion verifiers share one internal Pint registry using packaged definitions. Unknown units, incompatible dimensions, invalid offset-unit operations, non-finite values, custom definitions, and unit-definition paths fail explicitly.
