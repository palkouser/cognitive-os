# Tool Plane

Every tool is an untrusted capability. Execution always follows registry lookup, input validation,
policy evaluation, approval when required, controlled execution, output validation, artifact
handling, and lifecycle persistence. Provider proposals and MCP metadata never bypass this flow.
