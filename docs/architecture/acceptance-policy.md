# Acceptance policy

Required requirements must pass with an allowed outcome and minimum score. Missing, unsupported, unverifiable, and error outcomes are classified separately. Optional weights produce a deterministic score. Repairability is configured by trusted finding code, never by provider output. Acceptance decisions are immutable controller audit events.
