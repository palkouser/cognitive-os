# Gate L2 assessment — Pairwise Contrastive Selective Ranking (Sprint 21D5)

- Sprint: 21D5
- Assessed: 2026-08-09
- Branch: `feature/sprint-21d5-pairwise-selective-ranking`
- Parent baseline: `sprint-21d4-evidence-baseline`, tag object
  `0f1e4c897c72cedc530bb599c4d7af8e647b2774`, peeled to
  `18564a55e65f7b331bc73fc334ee1ab085cf0836`
- Migration head: `0015`, unchanged. `0016` remains unallocated.
- Revision: 5 — **provisional**. Condition 29 is `pending` until the protected merge, its
  exact-head post-merge `main` CI run and remote verification of the annotated tag
  `sprint-21d5-evidence-baseline`. The gate-close regeneration at S21D5-095 is what decides it.
- Frozen gate contract: `9e47bc618fc1eca8b66146eacdf1bd244fced79bb1c91f46f2c6ff4484bfd8a7`,
  29 conditions, 0 thresholds changed — the D4 contract, carried unchanged
- Machine-readable form: [`sprint-21d5-gate-l2.json`](evidence/sprint-21d5-gate-l2.json)
- Generated by `scripts/gate_assessment_d5.py`, which has no path that writes `met` without a
  document behind it and computes the verdict from the counts

This document does **not** replace [`gate-l2-assessment.md`](gate-l2-assessment.md),
[`gate-l2-d3-assessment.md`](gate-l2-d3-assessment.md) or
[`gate-l2-d4-assessment.md`](gate-l2-d4-assessment.md). Those are Sprint 21D2's, 21D3's and
21D4's historical assessments and stay exactly as they were written.

## Verdict

**Gate L2 does not pass.** Sprint 22A remains blocked.

| State | Conditions |
|---|---:|
| `met` | 13 |
| `met_as_rejection` | **0** |
| `not_opened` | 15 |
| `pending` | 1 |
| `failed` | **0** |
| `carried` | **0** |

Zero failures and no pass are not in tension. The gate passes when every applicable condition is
met; fifteen were never opened and one awaits the release that has not happened yet.

Two counts moved against D4, in opposite directions and for the same reason — D5 measured what
D4 recorded as unreachable:

- **`met_as_rejection` is zero.** D4 used it once, for condition 24: the floors were measured, no
  arm cleared them, and the recorded no was what the condition asked for. D5 measured the same
  floors on its own freshly authored holdout and the `lexical` arm cleared both, so 24 is `met`
  on a measurement rather than on a refusal to reopen. **Gate D1 condition 15 closes with it.**
- **`not_opened` is still fifteen, and it is the same fifteen.** The correction branch reached a
  different stop than D4's — `selective_margin_bound` under §3.3 step 5 rather than
  `hypothesis_class_bound` — and a different stop with the same dependents closes the same set.
  The set is not this document's to choose: `scripts/gate_assessment_d5.py` reads it out of
  `sprint-21d5-continuation.json` and refuses to run if the two disagree.

`carried` is zero on purpose. §2.2 is explicit that D5 inherits no pass from D4 — the thirteen
conditions D4 met had to be re-evidenced against D5's own authorities, corpora and campaigns, and
they were. The state exists in the vocabulary so that a reader can see it was not used.

## The two branch results

**Correction — a typed stop.** On 100 freshly authored independent calibration decisions, at 320
and 720 fitting rows, the fitted direction reaches first-choice 0.91 and 0.88 against a 0.42
deterministic baseline, and zero-error coverage 0.26 and 0.27 against §2.3's floor of 0.40, with
**exactly zero confident errors** in both cells. Both cells fail exactly one of §2.3's eight
conditions and satisfy the other seven. §3.3 **step 5, `selective_margin_bound`**: coverage above
zero rules out step 6, and flatness across a 2.25× volume span rules out step 4. Stop hash
`7b59897d8d83a51be3d8fb5c65e4208ddb07d813884eb99ccdb36b73236fec59`, 26 dependent items and these
fifteen conditions bound to it.

**Retrieval — a pass.** Sixty freshly authored unseen-task queries, read once, under the complete
searchable surface. The `lexical` arm reached Recall@5 0.7500 and MRR@10 0.5389 against floors of
0.70 and 0.50 and a chance baseline of 0.5768 / 0.3317. **Condition 24 met; Gate D1 condition 15
closed.** This is the sprint's one closed condition and it survives the correction stop, which is
what §8.2 requires of a valid independent result.

## Conditions

| Condition | State | Decided by | Detail |
|---:|---|---|---|
| 1 | `met` | `sprint-21d5-baseline.json` | the D4 tag object 0f1e4c897c72cedc peels to 18564a55e65f7b33, and the local and remote handles agree |
| 2 | `met` | `sprint-21d5-authority-isolation-after.json` | 6 predecessor stores re-fingerprinted after the wave against sprint-21d5-baseline.json; none drifted and zero writes reached one |
| 3 | `met` | `sprint-21d5-reuse-audit.json` | the 180-group fitting pool is 80 D4 training and 100 D4 calibration groups, read from sprint-21d4-holdout-reuse-audit.json at 2f25dc1716dd; 3 carried roles reproduce their released bundle digests, 65 protected task identities were resolved by identity alone, and zero protected bodies were opened |
| 4 | `met` | `sprint-21d5-pre-registration.json` | revision 5 published with measured_values: 0 |
| 5 | `met` | `sprint-21d5-calibration-campaign.json` | all 400 candidate runs carry an independent hidden-verifier label |
| 6 | `met` | `sprint-21d5-snapshots.json` | 390 columns, all on the correction-ranking-v2 allowlist; all 9 required scans passed |
| 7 | `met` | `sprint-21d5-corpus-separation.json` | 7 roles over 465 groups, all pairwise disjoint; 21 role pairs share zero groups |
| 8 | `met` | `sprint-21d5-snapshots.json` | 720 fitting observations over 180 groups and 400 calibration observations over 100 groups |
| 9 | `met` | `sprint-21d5-snapshots.json` | zero REAL_GOVERNED_RUN observations in fitting and calibration: {'training': 0, 'calibration': 0} |
| 10 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured final A and B, each 120 new verifier-backed outcomes over 30 groups |
| 11 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured one artifact selected before final access, with final manifests inaccessible to fitting |
| 12 | `met` | `sprint-21d5-learner-selection.json` | the strongest deterministic rung is fixed_input_order at 0.42, measured on the same decisions; all 2 cells and 200 sweep points are reported, including the ones that are not selectable |
| 13 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured at least 20 final group decisions differing from the strongest baseline |
| 14 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured at least 5 absolute points or 20% relative error reduction on final evidence |
| 15 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured the paired group bootstrap at seed 21041 over 2,000 resamples |
| 16 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured a positive learned-minus-baseline direction in both final batches |
| 17 | `met` | `sprint-21d5-operations.json` | 14 decision counts and 11 named denominators across 7 files, every rate over independent_decisions |
| 18 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured zero accepted-to-rejected safety, governance, permission, secret or destructive changes |
| 19 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured no retained domain losing more than 2 points and aggregate loss at most 1 point |
| 20 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured at least 100 pre-registered promotion metamorphic/OOD ranking decisions with exactly zero confident errors. Its own precondition was measured and did not clear: over 100 independent fresh calibration decisions the zero-error operating point admits 0.26 of them at 320 fitting rows and 0.27 at 720, against §2.3's floor of 0.40, with zero confident errors in both. The margin certifies honestly and does not certify enough |
| 21 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured shadow mode changing zero executed decisions against final evidence |
| 22 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured the selected artifact as canonical inert JSON with complete lineage. `CorrectionArtifactPayloadV3` and its loader are released and exercised, but no candidate names an operating point to bind into one, and a record about a fixture is not a record about a candidate |
| 23 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured the runtime resolver reaching every reason code against the real artifact, each with an immediate deterministic fallback |
| 24 | `met` | `sprint-21d5-retrieval-decision.json` | the lexical arm cleared Recall@5 >= 0.70 and MRR@10 >= 0.50 on 60 unseen queries read once; the condition is recorded as met |
| 25 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured a hash-bound canary manifest with the verifier mandatory and the kill switch immediate |
| 26 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured activation, loading, disable, restoration and rollback surviving restart on a real one |
| 27 | `not_opened` | `sprint-21d5-learner-selection.json` | would have measured an exact human approval over the existing fields, with no self-approval |
| 28 | `met` | `sprint-21d5-verification-matrix.json` | 32 of 32 release-matrix rows passed, 0 skipped, 0 structural findings |
| 29 | `pending` | `—` | the protected merge, its exact-head post-merge main CI, the annotated tag and the remote verification. The gate-close regeneration is what decides this row |

| Gate D1 | State | Closure rule | Detail |
|---:|---|---|---|
| 6 | `not_opened` | at least 200 unique eligible verifier-backed primary-surface outcomes | closed by the selection stop: the outcomes that would close it are final and canary outcomes, and neither was authorised |
| 7 | `not_opened` | at least 20 primary-surface examples change the advisory action | closed by the selection stop: condition 13's evidence read against the D1 contract, and condition 13 never opened |
| 15 | `closed` | a new retrieval holdout reaches both floors | measured and reached on sixty freshly authored unseen-task queries read once |

## What `not_opened` means here

Fifteen conditions are marked `not_opened` rather than failed or skipped, and the distinction is
the whole point of the state. Nothing about them was measured and found wanting: their evidence
would have been produced by waves that a pre-registered stop closed before they could open. Each
row binds the same stop hash, so a reader can check that fifteen rows were closed by one decision
rather than by fifteen judgement calls.

Condition 20 is the one worth reading in full. Its precondition *was* measured, and the
measurement is the stop: over 100 independent fresh calibration decisions the zero-error
operating point admits 0.26 of them at 320 fitting rows and 0.27 at 720, with zero confident
errors in both. The margin certifies honestly. It does not certify enough.

Condition 22 is the second. `CorrectionArtifactPayloadV3` is released, its loader is exercised,
and the sprint still cannot claim it: no candidate names an operating point to bind into an
artifact, and a record about a fixture is not a record about a candidate. D4 declined to blur
that distinction and D5 declines to blur it in the other direction.

## Gate D1

Condition 15 closes on S21D5-046, on D5's own evidence. Conditions 6 and 7 stay closed behind the
selection stop: both are measured on final and canary outcomes, and neither role was authorised.

## What this assessment does not claim

- **No condition is met against a fixture.** Where a fixture was the only thing available — the
  loader damage cases in S21D5-084 — the record names it `d3_contract_fixture` in its first field
  and no condition is decided from it.
- **No threshold moved.** `thresholds_changed: 0`, and the gate contract hash is D4's.
- **A passing retrieval floor is one surface, on authored tasks, under a frozen encoder.** It
  says nothing about the correction branch, and the sprint report's limitations section says so
  at length.
- **Condition 29 is not decided here.** It is `pending`, and a `pending` row is neither a stop nor
  a pass. The regeneration after the protected release is the only thing that can move it.
