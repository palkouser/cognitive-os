# Gate L2 assessment — Selective Correction Ranking (Sprint 21D4)

- Sprint: 21D4
- Assessed: 2026-08-08
- Branch: `feature/sprint-21d4-selective-correction-ranking`
- Parent baseline: `sprint-21d3-evidence-baseline`, tag object
  `bcf2976dd0f063b1eb4ea16b388eea590e6172dd`, peeled to
  `ef4388b1bf9cb842b25a06aa2255abd1042702c2`
- Migration head: `0015`, unchanged. `0016` remains unallocated.
- Revision: 4 — **provisional**. Condition 29 is `pending` until the protected merge, its
  exact-head post-merge `main` CI, and remote verification of the annotated tag
  `sprint-21d4-evidence-baseline`.
- Frozen gate contract: `9e47bc618fc1eca8b66146eacdf1bd244fced79bb1c91f46f2c6ff4484bfd8a7`,
  29 conditions, 0 thresholds changed
- Machine-readable form: [`sprint-21d4-gate-l2.json`](evidence/sprint-21d4-gate-l2.json)

This document does **not** replace [`gate-l2-assessment.md`](gate-l2-assessment.md) or
[`gate-l2-d3-assessment.md`](gate-l2-d3-assessment.md). Those are Sprint 21D2's and Sprint 21D3's
historical assessments and stay exactly as they were written.

## Verdict

**Gate L2 does not pass.** Sprint 22A remains blocked.

| State | Conditions |
|---|---:|
| `met` | 12 |
| `met_as_rejection` | 1 |
| `not_opened` | 15 |
| `pending` | 1 |
| `failed` | **0** |
| `carried` | **0** |

Zero failures and no pass are not in tension. The gate passes when every applicable condition is
met; fifteen were never opened, one was measured and rejected, and one waits on the release that
publishes this document.

`carried` is zero on purpose. §2.2 is explicit that D4 inherits no pass from D3 — the fifteen
conditions D3 met as infrastructure claims had to be re-evidenced against D4's own authorities or
recorded as not opened. Twelve were re-evidenced. Three that D3 met against a contract fixture it
built — 20, 22 and 23 — are `not_opened` here, because W4 declined to build a second fixture and
a record about a fixture is not a record about a candidate.

## What closed the fifteen

One stop, named in every closed row:
`5caa48970898d180ce1f339771399f42af74555a91af2f87e97d1f36c6086c8e` — S21D4-039's null candidate
selection. The grid carries signal and cannot be made selective: all 144 cells beat the strongest
deterministic baseline, and not one reaches zero confident errors on a non-empty admitted set at
either volume.

Condition 20 deserves its own sentence, because it is the condition the erratum was about. Its
promotion half never opened. Its *precondition* — the zero-error claim certified on at least 100
independent calibration decisions — was measured, on exactly 100 independent decisions, and
returned no point. That measurement is the stop that closed everything below it.

## What the one rejection means

Condition 24 is `met_as_rejection`: the retrieval floors were measured once, on sixty distinct
unseen queries under the widened surface, and no arm cleared both. Reciprocal rank fusion cleared
Recall@5 at 0.7500 against a floor of 0.70 and missed MRR@10 at 0.4911 against a floor of 0.50 —
**0.0089**. Under first-failure precedence that is a near miss, not a pass, and nothing was
reopened to close it: fusion variants 0, widths 0, weights 0, metrics 0, holdout members added 0.

## The condition table

Generated by `scripts/gate_assessment_d4.py` from the frozen gate contract and the produced
evidence. Every row names the file and rule that decided it, or the stop hash that closed it. The
script has no path that asserts a pass — it can only read one.

| Condition | State | Decided by | Detail |
|---:|---|---|---|
| 1 | `met` | `sprint-21d4-baseline.json` | the D3 tag object bcf2976dd0f063b1 peels to ef4388b1bf9cb842, and the local and remote handles agree |
| 2 | `met` | `sprint-21d4-authority-isolation.json` | 5 predecessor stores fingerprinted before and after; unchanged |
| 3 | `met` | `sprint-21d4-d3-reconciliation.json` | D3's 120 metamorphic decisions recomputed as 20 independent and 100 replicated, from D3's own evidence |
| 4 | `met` | `sprint-21d4-pre-registration.json` | revision 4 published with measured_values: 0 |
| 5 | `met` | `sprint-21d4-calibration-campaign.json` | all 400 candidate runs carry an independent hidden-verifier label |
| 6 | `met` | `sprint-21d4-snapshots.json` | 390 columns, all on the correction-ranking-v2 allowlist; 11 of 11 scans passed |
| 7 | `met` | `sprint-21d4-separation.json` | 6 roles, all pairwise disjoint; 15 role pairs share zero groups |
| 8 | `met` | `sprint-21d4-snapshots.json` | 320 fitting observations over 80 groups and 400 calibration observations over 100 groups |
| 9 | `met` | `sprint-21d4-snapshots.json` | zero REAL_GOVERNED_RUN observations in fitting and calibration: {'training': 0, 'calibration': 0} |
| 10 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured final A and B, each 120 new verifier-backed outcomes over 30 groups |
| 11 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured one artifact selected before final access, with final manifests inaccessible to fitting |
| 12 | `met` | `sprint-21d4-learner-selection.json` | the strongest deterministic rung is lexical_similarity, measured on the same decisions, and all 144 of 144 frozen cells are reported |
| 13 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured at least 20 final group decisions differing from the strongest baseline |
| 14 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured at least 5 absolute points or 20% relative error reduction on final evidence |
| 15 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured the paired group bootstrap at seed 21041 over 2,000 resamples |
| 16 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured a positive learned-minus-baseline direction in both final batches |
| 17 | `met` | `sprint-21d4-operations.json` | 177 decision counts and 29 named denominators across 10 files, every rate over independent_decisions |
| 18 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured zero accepted-to-rejected safety, governance, permission, secret or destructive changes |
| 19 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured no retained domain losing more than 2 points and aggregate loss at most 1 point |
| 20 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured at least 100 pre-registered promotion metamorphic/OOD ranking decisions with exactly zero confident errors. Its own precondition was measured and returned nothing: the zero-error operating point was derived over 100 independent calibration decisions at both volumes and no cell reached zero confident errors on a non-empty admitted set, which is the stop that closed everything below it |
| 21 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured shadow mode changing zero executed decisions against final evidence |
| 22 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured the selected artifact as canonical inert JSON with complete lineage. D3 met this against a contract fixture it built; W4 declined to build a second one, because a record about a fixture is not a record about a candidate and §2.2 gives D4 no pass to inherit |
| 23 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured the runtime resolver reaching every reason code against the real artifact, each with an immediate deterministic fallback. S21D4-075 proved restoration and rollback on the isolated lifecycle fixture, which is a different claim and is recorded as one |
| 24 | `met_as_rejection` | `sprint-21d4-retrieval-decision.json` | the floors were measured on 60 unseen queries and no arm cleared them; the first failed floor is mrr_at_10, and nothing was reopened to close it |
| 25 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured a hash-bound canary manifest with the verifier mandatory and the kill switch immediate |
| 26 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured activation, loading, disable, restoration and rollback surviving restart on a real activation. The rollback and refusal halves were proved on the isolated fixture by S21D4-075; no real activation existed to prove the rest against |
| 27 | `not_opened` | `sprint-21d4-learner-selection.json` | would have measured an exact human approval over the existing fields, with no self-approval |
| 28 | `met` | `sprint-21d4-verification-matrix.json` | 30 of 30 release-matrix rows passed, 0 skipped, 0 structural findings |
| 29 | `pending` | `—` | the protected merge, its exact-head post-merge main CI, the annotated tag and the remote verification. The gate-close regeneration is what decides this row |

| Gate D1 | State | Closure rule | Detail |
|---:|---|---|---|
| 6 | `not_opened` | at least 200 unique eligible verifier-backed primary-surface outcomes | closed by the selection stop: the outcomes that would close it are final and canary outcomes, and neither was authorised |
| 7 | `not_opened` | at least 20 primary-surface examples change the advisory action | closed by the selection stop: condition 13's evidence read against the D1 contract, and condition 13 never opened |
| 15 | `remains_open` | a new retrieval holdout reaches both floors | measured and not reached: the fusion arm cleared Recall@5 at 0.7500 and missed MRR@10 at 0.4911 against a floor of 0.50 |

## Gate D1

Conditions 6 and 7 are closed by the same selection stop. Condition 15 is not: the retrieval
branch ran to completion and returned a hash-bound negative result, so it **remains open** on its
own evidence rather than on someone else's stop. That distinction is the point of running the two
branches independently.

## What this assessment does not do

- It does not unblock Sprint 22A.
- It does not describe any W4 or W7 capability as exercised against a real candidate. D4 fitted
  no artifact; every surface that would have touched one is recorded as not opened.
- It does not reinterpret a D3 pass into a D4 one.
- It asserts nothing. Every `met` row names a file, a rule and a measured value, and every closed
  row names the hash of the record that closed it.
