# Benchmark datasets

Large or externally licensed benchmark datasets are stored outside Git under the configured Cognitive OS data root. Tracked manifests record source, revision, license, content hash, and case count. Importers never download, clone, or extract external data automatically.
