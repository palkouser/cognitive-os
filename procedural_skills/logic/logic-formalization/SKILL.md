# Logic Formalization

Map every statement to a typed node in the closed logic AST. Raw solver text is never produced
and never accepted; anything not expressible in the AST is rejected as unsupported.
