# Constraint Solving

Enumerate when the variable count is inside the row ceiling, otherwise escalate to the bounded
SMT tool. Report solver `unknown` and timeout as inconclusive, never as unsatisfiable.
