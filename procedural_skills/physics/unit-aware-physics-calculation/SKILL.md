# Unit-Aware Physics Calculation

Attach units to every quantity before calculating, convert only between compatible dimensions,
and convert offset temperature units explicitly. Unit metadata must never be dropped.
