# Exact Arithmetic Decomposition

Split the expression into its smallest exact operations, evaluate each over the rationals, and
keep every intermediate value. Never substitute a float for an exact value, and never round an
intermediate result to make a later step succeed.
